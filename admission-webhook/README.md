## ThreatMapper admission webhook for kubernetes

### notes
- install admission controller in deepfence namespace
- update deepfence console url and key in webhook-secret.yaml(manifests/webhook-secret.yaml)